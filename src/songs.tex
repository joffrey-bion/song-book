\documentclass[a4paper,11pt,french]{article}

% --- General packages ---------------------------------------------------------
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[francais]{babel}
\usepackage{graphicx}

% --- Specific packages --------------------------------------------------------

\usepackage{xargs}    % for x-macros with multiple optional arguments

\usepackage{libs/gchords}  % for nice chord diagrams [required BEFORE fancyhdr]
\usepackage{fancyhdr} % for fancier headers
\usepackage{vmargin}  % for margins fine-tuning (do not combine with fullpage)
\usepackage{makeidx}  % to create the index
\usepackage{hyperref} % to transform refs into links (\ref, toc, index...)

\usepackage{xcolor}   % for color management
\usepackage{multicol} % for multi-columns environments
\usepackage{amsmath}  % for text inside math stuff (for chords)
\usepackage{diagbox}  % for table cells cut diagonally

% --- Page settings ------------------------------------------------------------

\setlength{\parindent}{0mm}

\setmarginsrb{15mm}{9mm}{15mm}{10mm}{26pt}{8mm}{12pt}{11mm}
% Arguments:
% margins left, up, right, down
% header height
% distance between header and text
% footer height
% distance between text and footer

\renewcommand\headrulewidth{0pt} % removes the line below the header
\pagestyle{fancy}

% --- Specific macros and environments -----------------------------------------

\input{libs/songs-commands.tex}

\newcommand{\todo}{}

% --- Configuration ------------------------------------------------------------

\makeindex % creates the index
\hyphenation{} % declares wrongly hyphened words with dashes at the proper position

% --- Title page elements ------------------------------------------------------

\newcommand*{\HRule}{\rule{\linewidth}{0.4mm}}  % thick horizontal lines
\newcommand*{\auteur}[2]{\large #1~\textsc{#2}} % styles the author's first and last name
\newcommand{\pretitre}{Lyrics \& Chords}
\newcommand{\grostitre}{An Ink Drop For A Song}
\newcommand{\auteurs}{\auteur{Joffrey}{Bion}}
\newcommand{\madate}{\the\year} % \today to put the compilation date


\begin{document}


% --- Custom front page --------------------------------------------------------
\begin{titlepage}
  \begin{center}
    ~
    \vfill
    {\LARGE\pretitre\\}           % pre-title
    \vspace{2cm}
    \HRule \\[0.4cm]
    {\Huge\bf\grostitre\\[0.4cm]} % big title
    \HRule \\[0.4cm]
    \vspace{2cm}
    \auteurs\\                    % author(s)
    \vfill
    \vfill
    {\large\madate}               % date
  \end{center}
\end{titlepage}



% CONVENTIONS

% Titles :
%    Capital first letter
%    Capitalize each word's first letter (except the, of, le, la, de, des...)

% Ordering:
%    Sorted by artist name (If the artist has first and last name, first name is used first)
%    Sorted by song title within the same artist

% File naming:
%    artist-name_song-title-in-a-few-words.tex (in songs folder)

{{INCLUDES}}

\printindex


\end{document}

